#include "image.h"

// Define the image data (example data, fill with actual pixel values)
const CRGB image[64 * 32] = {
    CRGB::Red, CRGB::Green, CRGB::Blue, // Fill with 64*32 CRGB colors
    // ... continue for 64*32 pixels
};
